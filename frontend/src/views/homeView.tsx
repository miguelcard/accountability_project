import React from 'react'
import Home from '../components/Home/Home'

// are this views really needed? what is their functionallity? -> should more content go in here then?
const HomeView: React.FC = () => {
    return (
        <div>
            <Home></Home>
        </div>
    )
}

export default HomeView
